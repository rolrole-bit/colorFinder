# UI 필요 여부
- 추가 UI 컴포넌트는 없으나, 기존 UI의 레이아웃과 스타일을 개선함.

# 사용자 흐름
사용자가 점수 확인 -> 가독성 좋은 패널 내에서 본인의 점수 및 랭킹을 한눈에 파악.

# 화면 또는 명령 흐름
- ScoreboardView 진입 -> 2개의 주요 블록(점수, 랭킹)이 각각 반투명 둥근 사각형 안에 렌더링됨 -> 공유 또는 다시하기.

# core 모듈과의 연동 방식
N/A (순수 UI 계층 렌더링 변화)

# 상태 처리
N/A

# 에러 표시
N/A

# 검증 방법
- 배경색이 무엇이든(밝거나 어두운 그라디언트) 관계없이, 텍스트가 항상 흰색으로 렌더링되고 어두운 패널 덕분에 잘 읽히는지 시각적으로 검증.

# 확장 가능성
- 추후 내용이 길어지거나 요소가 추가되더라도 패널 내부에 포함하면 되므로 유지보수 용이함.
